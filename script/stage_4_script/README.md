## Stage 4 Script

This folder contains the script files for the text classification and text generation tasks.

**Text Classification**:
- Arya: no script files as code in `local_code/` can run the model (not final model)
- **Luna**: no script files as code in `local_code/` can run the model (final models)
- Ryan: contains the script file (not final model)

**Text Generation**:
- **Jeslyn**: contains the script file for RNN, GRU, and LSTM models. Note: Chenxuan's GRU model also uses the same script file (final model)
