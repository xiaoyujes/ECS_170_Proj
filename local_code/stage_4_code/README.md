## Stage 4 Code

This folder contains the local code for the RNN, GRU, and LSTM models for both the text classification and text generation tasks.

**Text Classificiation**:
- Arya: contains the code for the text classification task; the model doesn't contain a script file, so it can be directly run (model not used)
- **Luna**: contains all the code for the text classification task; the model doesn't contain a script file so it can be directly run (final text classification model)
- Ryan: contains all files needed for the text classification script (model not used)

**Text Generation**:
- **Chenxuan**: contains the method file for the GRU model; uses the same local code files as Jeslyn's models (final tuned model)
- **Jeslyn**: contains all local code files to be called in the script file
